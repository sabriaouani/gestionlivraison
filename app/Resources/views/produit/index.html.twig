{{ include('hf/header_gestionnaire.html.twig') }}
<br>
<!--<style>
    body {
        background-image: url("{{ absolute_url(asset('Bundles/rrrrrrrrrrrrrr.jpg')) }}");
        background-color: #cccccc;
    }

</style> -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.css">
<link rel="stylesheet" href="https://cdn.datatables.net/1.10.19/css/dataTables.bootstrap4.min.css">
<script>
    $('#myModal').on('shown.bs.modal', function () {
        $('#myInput').trigger('focus')
    });
    $(document).ready(function() {
        $('#example').DataTable();
    } );

</script>

<script src="https://cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.10.19/js/dataTables.bootstrap4.min.js"></script>

<div class="container">
    <div class="row">
        <a href="{{ path('produit_new') }}" class="btn btn-primary" style="position: absolute; right: 10px;">Ajouter Produit</a>
    </div>
    <br>
    <h1   style="color:black;">Listes des Produits:</h1>
    <table id="example" class="table table-striped" style="width:100%">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom de produit</th>
                <th>Type de produit</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}


            <tr>
                <td><a href="{{ path('produit_show', { 'id': produit.id }) }}">{{ produit.id }}</a></td>
                <td>{{ produit.nomProduit }}</td>
                <td>
                   {{ produit.idType }}
                </td>
                <td>
                    <a href="{{ path('produit_edit',{'id':produit.id})}}" class="btn btn-light">Modifier</a>
                   <a href="{{ path('produit_delete',{'id':produit.id})}}" class="btn btn-danger btn-sm"
                      data-toggle="modal" data-target="#{{'id' ~produit.id}}">Supprimer</a>
                    <div class="modal fade" id="{{'id' ~produit.id}}" tabindex="-1" role="dialog"
                         aria-labelledby="exampleModalLabel" aria-hidden="true">
                        <!-- Central Modal Small -->

                            <!-- Change class .modal-sm to change the size of the modal -->
                            <div class="modal-dialog modal-sm" role="document">


                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h4 class="modal-title w-100" id="myModalLabel">Vous pouvez pas supprimer ce produit</h4>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">

                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary btn-sm" data-dismiss="modal">ok</button>
                                    </div>
                                </div>
                            </div>
                        </div>

                </td>

            </tr>


        {% endfor %}
        </tbody>
        <tfoot>
        <tr>
            <th>Id</th>
            <th>Nom de produit</th>
            <th>Type de produit</th>
            <th>Actions</th>
        </tr>
        </tfoot>
    </table>
</div>

{{ include('hf/footer.html.twig') }}

