{{ include('hf/header_gestionnaire.html.twig') }}

<h1>Produit creation</h1>

{{ form_start(form) }}
        {{ form_widget(form) }}
<input type="submit" value="Create" class="btn btn-primary" />
{{ form_end(form) }}

<a href="{{ path('produit_index') }}" class="btn btn-primary ">Back</a>

{{ include('hf/footer.html.twig') }}

