{{ include('hf/header_admin.html.twig') }}

<h1>Gestionnaire creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary" />
    {{ form_end(form) }}

<a href="{{ path('gestionnaire_index') }}" class="btn btn-primary ">Back</a>

{{ include('hf/footer.html.twig') }}
