{{ include('hf/header_admin.html.twig') }}
<style>
    body {
        background-image: url("{{ absolute_url(asset('Bundles/bibliotheque1-1920x1027.jpg')) }}");
        background-color: #cccccc;
    }
</style>
<div class="container">

<h1>Modifier gestionnaire</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
<input type="submit" value="Modifier" class="btn btn-primary"/>

    {{ form_end(edit_form) }}
    <a href="{{ path('gestionnaire_index') }}" class="btn btn-primary btn-sm">Retour</a>

</div>

{{ include('hf/footer.html.twig') }}
